#include <stdio.h>
#include <stdlib.h>

int main() {
    printf("Hello, World!\n");

    // Wait for the user to press any key before exiting
    printf("Press any key to exit...\n");
    system("pause");  // This will pause and wait for user input

    return 0;
}
